"""Programa que pide por teclado una palabra y hemos de decir si es un palíndromo o no."""

# Declaración de variables:c 

palabra = " " # Cadena que contendrá la palabra a analizar (para ver si es palíndromo o no).
longitudPalabra = 0 # Contendrá el tamaño de la palabra.
indice = 0 # Indice con el que recorremos la palabra.

# Programa principal: 

palabra = input("Introduce la palabra para ver si es palíndromo o no: ")
longitudPalabra = len(palabra) 
for indice in range (0,longitudPalabra):
    while()